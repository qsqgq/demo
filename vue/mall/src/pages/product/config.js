// const prefix = 'mall-product';
// const suffix = 'key';
// export const PRODUCT_DETAILS_KEY = `${prefix}-details-${suffix}`;
export const PRODUCT_DETAILS_UPDATE_TIME_INTERVAL = 1 * 24 * 60 * 60 * 1000;

export const sliderOptions = {
  direction: 'horizontal',
  loop: true,
  pagination: true,
  interval: 0
};
